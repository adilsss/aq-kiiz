import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  linearGradient: {
    width: 154,
    height: 196,
    borderRadius: 18,
  },
  innerContainer: {
    flex: 1,
    margin: 5,
    backgroundColor: "#fff",
    justifyContent: "flex-end",
    borderWidth: 5,
    borderColor: "white",
    borderRadius: 16,
  },
});
